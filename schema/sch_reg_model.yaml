id_self:                sch_reg_model
guid_self:              sch_1c7b2cf29a78456a89400883c6a4db29
copyright:              Copyright 2026 William Payne
license:                Apache-2.0

protective_mark:

  - id_mark:            mark_public
    guid_mark:          mark_0c96ccb7b7534574acf6ed42f9deba0f

title:                  Model register schema
brief:                  |
                        The shape of the register whose entries are
                        language models.
description:            |
                        Composes sch_register with sch_entry_model in
                        the pattern every register schema uses. Each
                        entry is the common register entry shape and
                        the model shape together, closed to anything
                        neither declares.
$schema:                https://json-schema.org/draft/2020-12/schema
$id:                    https://capability-commons.org/schema/sch_reg_model.yaml
usage:                  |
                        Named by reg_model as its schema.

allOf:
  - $ref:               https://capability-commons.org/schema/sch_register.yaml

  - type:               object

    properties:

      table:

        additionalProperties:

          allOf:
            - $ref:     https://capability-commons.org/schema/sch_entry.yaml
            - $ref:     https://capability-commons.org/schema/sch_entry_model.yaml

          unevaluatedProperties:  false

unevaluatedProperties:  false
