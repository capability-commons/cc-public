# RETIRED — do not use.
#
# This draft "vocabulary" type was proposed as the Grounding pass's output
# type for the requirements-engineering process design, then retired during
# cross-check against the sensor-simulation schema work
# (../CC_SCHEMA_DESIGN_NOTES_SENSOR_SIM.md): it duplicated the purpose of
# the existing glossary.schema.yaml, which already has real production
# content built against it (sensor_simulation.glossary.yaml).
#
# Resolution: the Grounding pass produces or updates a glossary item
# (glossary.schema.yaml) directly, typically with status: draft until
# reviewed, rather than a separate vocabulary type. See
# CC_SCHEMA_DESIGN_NOTES_IDENTITY_AND_TRACEABILITY.md for the fuller
# cross-check this decision came out of, and
# "SYNTHESIS - SOPHIST methodology distilled for LLM prompts.md" §14 in the
# requirements-engineering reference library for how this fits the wider
# multi-pass process design.
#
# This file could not be deleted from this session (file-delete
# permissions), so it's left as this stub rather than as a live,
# potentially-confusing duplicate schema. Safe to actually delete by hand.
