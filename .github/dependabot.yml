# A pinned action is a fossil unless something moves it. These pins are
# full commit shas, which cannot be moved under us, and which therefore
# never gain a fix unless a person raises them. This is what raises them.
#
version: 2

updates:

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: Update
